include(cc_library)
include(cc_test)

cc_library(
  NAME 
    model
  HDRS
    causal_lm.h
    rec_causal_lm.h
    causal_vlm.h
    dit_model.h
    mm_embedding_vlm.h
    model_input.h
    model_input_param_groups.h
    model_args.h
    model_input_params.h
  SRCS
    model_input.cpp
  DEPS
    :common
    :common_layers
    :kv_cache
    :parallel_state
    :processors
    :chat_template
    glog::glog
    torch
    torch_python
    $<$<BOOL:${USE_NPU}>:platform_npu>
)

cc_test(
  NAME
    model_input_test
  SRCS
    model_input_test.cpp
  DEPS
    model
    GTest::gtest_main
)
